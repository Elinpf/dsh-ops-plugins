# todo_tree-tree 领域词汇表

> 纯术语表，不含实现细节。每个术语对应原型中一个可见的视觉组件。

## 节点 (Node)

树上的一个点。每个节点有一个一句话描述、一个状态、所属轮次。

节点本身不区分类型——同一套状态机适用于所有节点。**目标** 是一种状态而非一种节点类型：一个节点可以处于 `goal` 状态表示"这是一个需要达成的目标"，处于 `step` 之类状态表示"这是一个具体动作"。两者的区别是**稳定性**：目标比步骤更稳定，不会频繁变化。

### 根节点 (Root)

调查的**起点**。即运维人员要解决的故障/问题本身。
- 位置：树的最顶部，主轨道
- 始终存在，从创建树时就有
- 图标：**黄色实心圆**（标记"从这里开始"）

### 最终目标 (Final Goal)

调查的**终点**。从创建树时就存在，用虚线连接。
- 位置：树的最底部
- 始终存在，状态 `goal` → `resolved`
- 用**虚线**连接到其上方最近的节点

### 里程碑 (Milestone)

调查过程中的**中间目标**——一个比步骤更稳定、有方向性的锚点。例如"确认根因是不是 OOM"、"排除网络层"、"验证服务恢复"。
- 不预设目标的类型（可以是验证、排除、达成某个状态等）
- 状态 `goal`，达成后 → `done`
- 比步骤更稳定，不会频繁变化
- 用虚线连接

## 状态 (Status)

节点的当前状态。同一套状态机适用于所有节点。

| 状态 | 图标 | 含义 |
|---|---|---|
| `goal` | ○ 虚线圆环（粗虚线） | 需要较强意向达成的目标。最终目标、里程碑等。比步骤稳定，不频繁变化。用虚线连接 |
| `pending` | ○ 虚线圆环（细虚线） | 待开始的步骤。变化频繁 |
| `in_progress` | ● 渐变圆环+旋转 | 进行中 |
| `done` | ✅ 实心圆+勾 | 完成 |
| `dead_end` | ✗ 圆圈+叉 | 走不通/不是根因。非终态，可重新探索，可挂子节点 |
| `resolved` | ★ 填充圆+勾 | 目标已达成。虚线变实线 |

### 死胡同 (Dead End)

某条路线被证实不可行。标记后**可重新探索**——agent 可从 dead_end 回到 in_progress，也可在 dead_end 下继续挂子节点。
- 保留在图上不删除，全程探索轨迹可查
- 视觉：圆点图标变红叉，卡片半透明+删除线
- **不是叶节点**——下面可以继续有子节点
- **不是终态**——可从 dead_end→in_progress 重新探索
- 不做级联——标记 dead_end 时不自动改变子节点状态

## 视觉组件

### 圆点 (Dot)

轨道线上的标记点。**唯一的状态图标载体**（卡片内不重复显示状态图标）。
- 携带状态图标
- 位于轨道线上，位置即节点在树中的深度

### 状态图标 (Glyph)

圆点内的 SVG 图标，表示节点当前状态。**图标保持各自语义颜色**，卡片文字和连接线使用统一颜色。
- `root` = 黄色实心圆（起点标记）
- `goal` = 绿色虚线圆环（粗虚线）
- `done` = 绿色实心圆+勾
- `in_progress` = 蓝色渐变圆环+旋转
- `pending` = 灰色虚线圆环
- `dead_end` = 红色圆圈+叉
- `resolved` = 绿色填充圆+勾

### 卡片 (Card)

圆点右侧的描述框，所有卡片统一起点（在所有轨道线右侧）。
- **收起态（默认）**：只有一行文字（任务描述）+ 轮次标签
- **展开态**：点击展开，显示详情（根因、修复、子步骤等）
- 不包含状态图标（状态由圆点承载）
- 卡片文字统一颜色和字体，不因状态变色
- 死胡同节点：文字删除线+半透明

### 轨道线 (Lane Line)

左侧的垂直线，表示一条轨道。所有轨道线在左侧窄区域排列，卡片统一在右侧。
- 主轨道线在最左
- 侧轨道线在右侧依次排列
- 轨道线在节点间连续可见，使分支结构清晰

### 轮次标签 (Turn Badge)

卡片内的小标签，格式 `T1,2`，标记哪些 turn 操作过该节点。

### 连接线 (Connector)

圆点之间的线。**所有连接线使用统一灰色**，不因状态或类型变色。
形态区分：

- **直线 (Linear)** — 线性步骤推进，同一轨道内垂直连接
- **曲线分叉 (Branch)** — 从一个轨道偏移到侧轨道
- **曲线合并 (Merge)** — 从侧轨道回到主轨道（或另一个轨道）
- **虚线 (Dashed)** — 连接到处于 `goal` 状态的节点。达成后（`resolved`）虚线变实线
- 死胡同的连接线也为虚线（统一灰色）

### 轨道 (Lane)

圆点所在的垂直列。
- **主轨道 (Main Lane)** — 位置 0。根节点和最终目标始终在此
- **侧轨道 (Branch Lane)** — 位置 1+。三种情况产生侧轨道：
  - **死胡同后** — 走到死胡同，从死胡同的父节点开新路线到侧轨道
  - **并行探索** — 从某节点同时分叉出多条路线，主轨道继续推进的同时侧轨道并行探索。可由主 agent 分叉或派子 agent 探索，子 agent 的结果作为侧轨道节点出现在同一棵树上
  - **探索分叉** — 主动分叉探索一条备选路线，侧轨道完成后可合并回主轨道（或继续推进）
- 侧轨道不限于一条；可以有多条并行的侧轨道，侧轨道也可以再分叉

### 并行子节点 (Parallel Children)

一个节点可以同时有多个活跃子节点。例如 B 同时分叉出 D（主轨道继续）和 C（侧轨道探索），C 走到死胡同不影响 D 继续推进。这与"死胡同后回溯再开新路线"不同——这里是**同时存在**的多条路线，其中一些可能死掉、一些继续。
