---
'@elinpf/dsh-ops-tool-kubectl': patch
---

Renamed the kubectl tool package from `@elinpf/dsh-ops-kubectl` to `@elinpf/dsh-ops-tool-kubectl`, aligning it with its siblings `@elinpf/dsh-ops-tool-ceph` and `@elinpf/dsh-ops-tool-ssh`. The old name is deprecated on npm and points here; no code changes beyond the package name.
